<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.progress.mapper.ProgressStateMapper">

    <resultMap id="ProgressStateMap" type="com.example.progress.model.ProgressState">
        <id property="id" column="id"/>
        <result property="behaviorName" column="behavior_name"/>
        <result property="stepPercent" column="step_percent"/>
        <result property="progressPercent" column="progress_percent"/>
        <result property="completedCount" column="completed_count"/>
    </resultMap>

    <!-- 只维护一条记录，取第一条即可 -->
    <select id="findOne" resultMap="ProgressStateMap">
        SELECT id,
               behavior_name,
               step_percent,
               progress_percent,
               completed_count
        FROM progress_state
        ORDER BY id
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.example.progress.model.ProgressState">
        INSERT INTO progress_state
        (id, behavior_name, step_percent, progress_percent, completed_count)
        VALUES
        (#{id}, #{behaviorName}, #{stepPercent}, #{progressPercent}, #{completedCount})
    </insert>

    <update id="update" parameterType="com.example.progress.model.ProgressState">
        UPDATE progress_state
        SET behavior_name    = #{behaviorName},
            step_percent     = #{stepPercent},
            progress_percent = #{progressPercent},
            completed_count  = #{completedCount}
        WHERE id = #{id}
    </update>

</mapper>

